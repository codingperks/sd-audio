# to fill in
# Joining together data pipeline and model pipeline


class Pipeline:
    def __init__(self, model):
        self._model = model

    def set_dirs(self, data_dir, checkpoint_dir, inf_dir):
        return

    def train(config):
        # big parse of config file
        return

    def inference(config):
        return
